services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: python -m uvicorn quantfolio.api.main:app --host 0.0.0.0 --port 8000
  dashboard:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run src/quantfolio/app.py --server.address 0.0.0.0 --server.port 8501
